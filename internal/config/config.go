// Package config хранит глобальные константы: размеры экрана, мира и базовый
// баланс. Всё, что «крутится» при настройке игры, живёт здесь — один пакет,
// от которого можно зависеть отовсюду (сам он не зависит ни от чего).
package config

const (
	// Логическое (внутреннее) разрешение — пиксель-арт рисуем в нём.
	ScreenW = 640
	ScreenH = 360

	// Реальный размер окна (кратен логическому — чёткие пиксели).
	WindowW = 1280
	WindowH = 720

	// Размер игрового мира (больше экрана — нужна камера). 2048 = 64 тайла по
	// 32 px — кратно 16 (регион map-spec) и совпадает с размером острова биома.
	WorldW = 2048
	WorldH = 2048

	// Тактовая частота логики.
	TPS = 60
)
